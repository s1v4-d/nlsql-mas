"""FastAPI application and routes.

The app should be imported directly from app module to avoid
import-time side effects:

    from retail_insights.api.app import app
"""

# Note: We don't import app here to avoid requiring environment
# variables at import time. Import directly from app module instead.
